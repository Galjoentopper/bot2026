# ==============================================
# Core Dependencies
# ==============================================

# Cryptocurrency Data Download
python-bitvavo-api>=1.0.0

# Data Processing
pandas>=1.5.0,<3.0.0
numpy>=1.21.0,<2.0.0

# ==============================================
# Machine Learning / Deep Learning
# ==============================================

# TensorFlow (for prediction models: LSTM, GRU, BiLSTM, DLSTM)
# OPTIMIZATION: Use tensorflow[and-cuda] for GPU support with CUDA 12.x
# This package includes TensorFlow with CUDA/cuDNN support pre-configured
# TensorFlow 2.15.0 works with CuDNN 9.1.0 and CUDA 12.x
# Note: On RunPod, you may need to set LD_LIBRARY_PATH for TensorFlow to find CUDA libraries
tensorflow[and-cuda]==2.15.0  # GPU support with CUDA 12.x and CuDNN 9.1.0
# Alternative if above doesn't work: tensorflow==2.15.0 (may need manual CUDA setup)

# Scikit-learn (for data preprocessing)
scikit-learn>=1.2.0,<2.0.0

# ==============================================
# Reinforcement Learning (PPO)
# ==============================================

# Stable Baselines3 (PPO implementation)
stable-baselines3[extra]>=2.0.0,<3.0.0

# Gymnasium (RL environment)
gymnasium>=0.28.0,<1.0.0

# Shimmy (Gym compatibility)
shimmy>=0.2.0,<1.0.0

# PyTorch (required by stable-baselines3)
# Note: Install appropriate version for your CUDA version
# For CUDA 12.1/12.4: Use --index-url https://download.pytorch.org/whl/cu121
# For CPU only: Remove --index-url flag
torch>=2.0.0,<3.0.0
torchvision>=0.15.0,<1.0.0
torchaudio>=2.0.0,<3.0.0

# ==============================================
# Visualization & Utilities
# ==============================================

# Visualization
matplotlib>=3.6.0,<4.0.0
seaborn>=0.12.0,<1.0.0

# Progress bars
tqdm>=4.65.0,<5.0.0

# ==============================================
# Logging & Monitoring
# ==============================================

# TensorBoard (for training visualization)
tensorboard>=2.13.0,<3.0.0

# System monitoring (optional, for runpod_main.py memory checks)
psutil>=5.9.0,<6.0.0
